# MySQL.  Versions 4.1 and 5.0 are recommended.
#
#
# Be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html
development:
    adapter: jdbcmysql
    username: root
    password: root
    host: localhost
    database: pizzatown_dev
    timeout: 5000
    encoding: utf8

test:
    adapter: jdbcmysql
    username: root
    password: root
    host: localhost
    database: pizzatown_dev_test
    timeout: 5000
    encoding: utf8

production:
    adapter: jdbcmysql
    username: root
    password: root
    host: localhost
    database: pizzatown_dev_production
    timeout: 5000
    encoding: utf8

